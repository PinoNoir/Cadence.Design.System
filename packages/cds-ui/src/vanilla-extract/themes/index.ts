import { tokens } from '..';
import { bccDarkThemeClass, bccLightThemeClass } from './bcc-theme.css';
import { base } from './globalTokens.css';

/**
 * Color tokens and "themes" (aka color modes)
 */
const themes = {
  bccLightThemeClass,
  bccDarkThemeClass,
};

export { themes, base, tokens };
